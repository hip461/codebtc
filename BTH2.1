

	#include <stdio.h>
	int main() {
		int n;
		printf("nhap n phai lon hon 0: \t");
		scanf("%d", &n);
		if (n < 0){
			printf("nhap n lon hon 0");
		}
		else {
			for (int i = 0; i < n; i += 2) {
				printf(" so chan la: %d\n", i);
			}
		}
		return 0;
	}
