#include <stdio.h>
int main() {
	int n;
	printf("moi b nhap n: ");
	scanf("%d", &n);
		if (n > 0) {
			printf("so n la so duong\n");
		}
		else {
			printf("so n la so am\n\a");
		}
		if (n % 2 == 0) {
			printf("%d la so chan\n", n);
		}
		else {
			printf("%d la so le\n", n);
		}
