int main() {
		int a, b, c, d;
		printf("moi ban nhap a: \n");
		scanf("%d", &a);
		printf("moi ban nhap b: \n");
		scanf("%d", &b);
		printf("moi ban nhap c: \n");
		scanf("%d", &c);
		printf("moi ban nhap d: \n");
		scanf("%d", &d);
		int max = (a > b) ? a : b;// điều kiện đúng gán a vào max nếu sai gán b vào
		max = (max > c) ? max : c;
		max = (max > d) ? max : d;

		if (a == b && b == c && c == d) {
			printf("Khong co gia tri lon nhat!\n");
		}
		else {
			printf("Gia tri lon nhat trong 4 so la: %d\n", max);
		}
		return 0;
	}
